

const PrimaryButton = ({children}) => {
    return (
        <div className="btn btn-ghost border-0 border-b-4 border-l-2 border-[#C9F31D] text-lg font-Space_Grotesk">
            {children}
        </div>
    );
};

export default PrimaryButton;